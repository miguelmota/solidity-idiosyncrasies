var StorageOverride = artifacts.require('./StorageOverride.sol')

contract('StorageOverride', function(accounts) {
  it('example', async function() {
    const instance = await StorageOverride.deployed()
  })
})
