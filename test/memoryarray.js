var MemoryArray = artifacts.require('./MemoryArray.sol')

contract('MemoryArray', function(accounts) {
  it('memory array', async function() {
    const instance = await MemoryArray.deployed()
  })
})
